# Vite environment variables for Supabase (do not commit real secrets)
VITE_SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
VITE_SUPABASE_ANON_KEY=YOUR_PUBLIC_ANON_KEY
# Optional: override Functions host; otherwise derived from VITE_SUPABASE_URL
VITE_SUPABASE_FUNCTIONS_URL=https://YOUR_PROJECT_REF.functions.supabase.co

# Dev-only auto login credentials (optional)
# Set these to automatically sign in during local development.
# Example:
# VITE_DEV_AUTO_LOGIN_EMAIL=dev.user@example.com
# VITE_DEV_AUTO_LOGIN_PASSWORD=supersecret
VITE_DEV_AUTO_LOGIN_EMAIL=
VITE_DEV_AUTO_LOGIN_PASSWORD=

# Optional: enable a dev-only preflight probe for functions (default off)
# When true, some clients may test the functions host reachability before calls
# Note: this can cause extra network noise; leave false unless needed
VITE_DEV_PROBE_FUNCTIONS=false
